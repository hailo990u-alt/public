{% doc %}
  Renders the canonical Shopify product title so names stay consistent across
  the home page, collection pages, product pages, cart, and search.

  @param {product} product - The product whose title should be rendered.
{% enddoc %}

{{ product.title }}
